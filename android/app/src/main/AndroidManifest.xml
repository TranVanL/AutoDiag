<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ============================================================
         Custom signature-level permission
         protectionLevel="signature": Only app has the same signature can grant this permission.
         ============================================================ -->
    <permission
        android:name="com.vdiag.permission.DIAGNOSE"
        android:label="Vehicle Diagnostic Access"
        android:protectionLevel="signature"/>

    <!-- ============================================================
         User app get permission from system
         ============================================================ -->
        <uses-permission android:name="com.vdiag.permission.DIAGNOSE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VDiag">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".service.DiagCarService"
            android:process=":car_service"
            android:exported="false"
            android:enabled="true">
        </service>
    </application>

</manifest>